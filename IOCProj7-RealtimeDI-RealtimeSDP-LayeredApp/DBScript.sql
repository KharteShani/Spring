CREATE TABLE `database1`.`laptopdetails` (
  `lapno` INT(10) NOT NULL AUTO_INCREMENT,
  `lapname` VARCHAR(45) NOT NULL,
  `lapprice` FLOAT(10) NOT NULL,
  `laplength` FLOAT(10) NOT NULL,
  `lapweight` FLOAT(10) NOT NULL,
  PRIMARY KEY (`lapno`));


